# Screen 02 — Setup Config · Arrive

**This screen is shown:**

- Only after **Screen 01**, following Player 1's input of the **START** button.

With hype music designated **Theme 03**.

This screen resembles **Screen 05 — Player Podium**, but it notably starts with
**no players behind the podiums yet**.

> **ROM correlation:** the podium-style layout shared with Screen 05 (and the
> board-style layout shared with Screen 03) is consistent with the engine reusing
> a small set of jump-table handlers for several scenes — see your "8 scenes"
> hypothesis in [`../ROADMAP.md`](../ROADMAP.md).
